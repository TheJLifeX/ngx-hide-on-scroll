import { NgxHideOnScrollDirective } from './ngx-hide-on-scroll.directive';

describe('NgxHideOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxHideOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
